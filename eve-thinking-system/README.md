# 🍎 Eve's Apple Tree Thinking System

Most note apps are filing cabinets: you put things in and they sit there flat. This one treats your notes as a **living tree of thought** and pairs you with an AI **Gardener** whose only job is to make you think more, think wider, and never lose a good idea. As you think, the tree grows **taller** (over time) and **wider** (across fields and links) — and you can fly around it in 3D. The point isn't organizing information; it's **growing a thinker**.

> **Fastest start (beginners): [`starter-vault/`](starter-vault/)** — a ready-to-open Obsidian vault with the plugin pre-bundled, the Gardener installed, and a real "My First Tree" waiting inside. Open it, read `START-HERE.md`, and you're planting in three steps. Everything below is the deeper reference.

---

## What's in the box

| Box | Folder / file | What it's for |
|---|---|---|
| **Method docs** | `GUIDE.md` · `HOW-TO.md` · `RULES.md` · `NOTE-FORMAT.md` | The method and its reasoning (`GUIDE`), your practical walkthrough (`HOW-TO`), the hard rules and the note schema. |
| **Templates + prompts** | `templates/` · `prompts/` | Note shapes (leaf, flower, apple, trunk, root, field) and ready-to-paste moves (inspire, extend, capture-aha, harvest, daily/weekly tend, quickstart). |
| **Agents kit** | `agents/` | The AI half: the **Gardener** skill plus two subagents (challenger, observer). See `agents/README.md`. |
| **Viz renderer** | `viz/` | The self-contained 3D "tree of light" — a template you feed data into, plus ready demos. See `viz/README.md`. |
| **The contract** | `CLAUDE.md` | The Gardener's operating contract. Drop this in a vault and any capable AI reads it and becomes your Gardener. |
| **Plugin** | `../plugin/` | The **Eve's Apple Tree** Obsidian plugin — renders your live vault as the 3D tree inside Obsidian. |

---

## The idea in one picture

- Every note is a **🍃 leaf** — one idea, titled as a short *handle* (a phrase or a raw question), not a finished thesis.
- Leaves grow in **fields** (lenses on how you think: *Researcher · Maker · Critic · Storyteller*) and rise over **time** (old at the bottom, new at the top).
- Connections are **branches** and — across fields — **rhizome** threads. The web of links *is* the structure. No rigid folders.
- A breakthrough is a **🌸 flower** (an *Ah-ha* you'll never lose); a finished thing is a **🍎 apple** (an output — you learn by producing).
- The deeper soil is your **🌱 root**: *how and why you think.*

---

## Your first 30 minutes

1. **See a tree first.** Open `viz/renaissance-demo.html` in a browser and fly around — scroll to zoom in (titles, then descriptions appear), pull back and the tree becomes a green silhouette in a forest. *(Or, to see your own vault live, install the Obsidian plugin in `../plugin/`.)*
2. **Give your AI the contract.** Either drop `CLAUDE.md` into an Obsidian vault opened with Claude Code, or install the Gardener as a skill (`agents/README.md`). Now the AI knows how to be your Gardener.
3. **Calibrate its voice.** Run the **DOT-VOICE** exercise: the Gardener hands you one short source and you hand-write 3–5 dots in your own words. It learns your voice and titles like you from then on.
4. **Plant 3 dots.** Just talk — say three thoughts. The Gardener turns each into a clean leaf, proposes a link, and hands you a question from a lens you skipped. Bloom the first Ah-ha.
5. **Look at your tree.** Render it and watch it stand up. Come back tomorrow and it's taller.

---

## Where to read next

- **`HOW-TO.md`** — your first session and the six moves in practice.
- **`GUIDE.md`** — the full method and *why* it works.
- **`agents/README.md`** — installing and running the Gardener + subagents.
- **`viz/README.md`** — how to render any tree (and the forest) without hand-rolling a renderer.

---

*Eve's Apple Tree Thinking System — a tool for thinking, not just filing. © 2026.*
