---
tree_type: seed
field:
---

<!-- A SEED is a topic you have NOT opened yet — a door, not an idea.
     Title it with the shortest handle that will still mean something to you in three months.
     Body: at most the question that made you park it. If you find yourself writing an
     argument here, it isn't a seed any more — it's a leaf, so give it a `time` and move it.

     field:  optional. With one, this seed pools at that tree's foot. Without one, it drifts
             to the shared forest pool. Never give a seed a `time` — it hasn't happened yet. -->

